version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/code/app
      - ./pension.db:/code/pension.db

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
